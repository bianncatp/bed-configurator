import React from 'react';
import ReactDOM from 'react-dom/client';
import BedConfigurator from './bed-configurator.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BedConfigurator />
  </React.StrictMode>
);
